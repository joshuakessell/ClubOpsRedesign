# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clubops
DB_USER=clubops
DB_PASSWORD=clubops_dev

# HTTP port
PORT=3001

# Register session TTL (seconds)
REGISTER_SESSION_TTL_SECONDS=900
# TTL cleanup interval (seconds)
REGISTER_SESSION_CLEANUP_INTERVAL_SECONDS=30
# Staff session TTL (seconds)
STAFF_SESSION_TTL_SECONDS=43200
